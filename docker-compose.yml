services:
  postgres:
    image: postgres:16
    container_name: depotiq-postgres
    environment:
      POSTGRES_DB: depotiq
      POSTGRES_USER: depotiq
      POSTGRES_PASSWORD: depotiq
    ports:
      - "5432:5432"
    volumes:
      - depotiq-postgres-data:/var/lib/postgresql/data

volumes:
  depotiq-postgres-data:

