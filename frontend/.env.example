# Use an empty API base URL when the Vite development proxy is enabled.
VITE_API_BASE_URL=

# Change this when Spring Boot is running on a non-default port.
VITE_API_PROXY_TARGET=http://localhost:8080
